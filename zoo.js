
//inventory object, item, quantity, date
//health records object, animal data, employee, date, medicine/food
//client object.events, ticketing, client data.
var events[];
var staff[];
var inventoryItem[];
var inventoryQty;
function Scheduling(events,staff) {
 if (staff.ava == 'Y') {
 	//asign to today's event
 }
}

function inventory(inventoryQty,inventoryItem) {
	if (inventoryQty<5) {
		reOrderItem(inventoryItem){
			var amountNeeded;
		};
	}
}

function healthRecords(animalId) {
	if (animalid=="Match") {
		//display animal health reccord.
	}
}
function client(events) {
var purchaseDate
 if (purchaseDate== events[i].Date) {
	//call Event schedule upsell query.
 }
}


if (clientLogin == true) {
	client();
}
else if (userLogin==true){
	Scheduling();
	if (animalHealth==true) {
		healthRecords();
	}else inventory();
}

